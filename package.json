{
  "name": "@madnesslabs/website",
  "private": true,
  "version": "1.0.0",
  "description": "The website for Madness Labs",
  "license": "MIT",
  "files": [
    "dist/"
  ],
  "scripts": {
    "build": "stencil build",
    "deploy": "npm run build && firebase deploy",
    "start": "stencil build --dev --watch --serve",
    "test": "stencil test --spec --e2e",
    "test.watch": "stencil test --spec --e2e --watch"
  },
  "dependencies": {
    "@ionic/core": "one",
    "@types/jest": "^24.0.14",
    "firebase": "~6.2.0"
  },
  "devDependencies": {
    "@stencil/core": "~1.1.6"
  }
}
